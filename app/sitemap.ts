import type { MetadataRoute } from "next"; import { blogPosts } from "./blog/articles"; import { services, site } from "./lib/site-data";
export default function sitemap(): MetadataRoute.Sitemap { const now=new Date(); const core=["","/projects","/about","/faq","/request-quote","/privacy"].map((path,i)=>({url:`${site.url}${path}`,lastModified:now,changeFrequency:(i===0?"weekly":"monthly") as "weekly"|"monthly",priority:i===0?1:.7})); const servicePages=services.map(s=>({url:`${site.url}/${s.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.85})); const posts=blogPosts.map(p=>({url:`${site.url}/blog/${p.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.6})); return [...core,...servicePages,...posts]; }
